function neighbors = getMobileNetwork_connectivity(ii_saved_local, neighbors, devices, epoch)
% returns a vector of length 'neighbors' that contains the indexes of
% neighbors (use NaN if the number of neighbors is lower than 'neighbors'
neighbors = NaN*ones(neighbors,1);

outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

